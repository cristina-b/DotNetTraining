using System.IO;

namespace StrategyFileEncrypt
{
    public class ChooseEncryption
    {
        IEncrypt algorithm;
        File file;

        public ChooseEncryption(IEncrypt algorithm)
        {        
            sortStrategy.Sort(list);      
            // to be continued
        }
    }
}